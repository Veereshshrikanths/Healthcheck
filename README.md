# Healthcheck
Is a simple Bash script that performs a quick system health check and logs key system metrics — such as CPU, memory, disk usage, uptime, and service status — to a log file named healthlog.txt with timestamps.
